export type DocumentsType = {
  id: number;
  fileName: string;
  fileSize: string;
  fileType: string;
  uploadDate: string;
  status: string;
  verificationDate: string;
  verificationStatus: string;
};

export type IUserType = {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
};
